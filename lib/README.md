This folder is for small helper modules, API clients and data-access code.

Examples:
- `apiClient.ts` — http wrapper
- `traces.ts` — functions to fetch/process TRACE data

Keep server-only code here if you need to separate concerns.