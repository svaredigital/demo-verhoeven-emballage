Global styles live in `app/globals.css` for Next.js App Router setups.

If you prefer moving global CSS to `styles/`, update the import in `app/layout.tsx`. For now we keep `app/globals.css` as-is to avoid breaking the app.